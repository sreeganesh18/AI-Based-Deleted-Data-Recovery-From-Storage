# =============================================================================
# .env.example — AI-Based Deleted Data Recovery
#
# INSTRUCTIONS:
#   1. Copy this file to .env:
#      Windows:  copy .env.example .env
#      Linux/Mac: cp .env.example .env
#
#   2. Edit the values below to match your local system.
#   3. NEVER commit your .env file to git — it is in .gitignore.
# =============================================================================

# ---------------------------------------------------------------------------
# Streamlit Port (HOST machine port)
# Change this if port 8501 is already in use on your machine.
# The UI will be accessible at: http://localhost:<STREAMLIT_HOST_PORT>
# ---------------------------------------------------------------------------
STREAMLIT_HOST_PORT=8501

# ---------------------------------------------------------------------------
# Checkpoints Directory (HOST path)
# Absolute or relative path to the folder containing trained model weights.
# Required files inside this folder:
#   - classifier_best.pth   (Fragment Classifier, ~50MB)
#   - autoencoder_best.pth  (Fragment Autoencoder, ~780KB)
#
# Windows examples:
#   CHECKPOINTS_DIR=C:/Users/YourName/models/checkpoints
#   CHECKPOINTS_DIR=./models/checkpoints
#
# macOS/Linux examples:
#   CHECKPOINTS_DIR=/home/yourname/models/checkpoints
#   CHECKPOINTS_DIR=./models/checkpoints
# ---------------------------------------------------------------------------
CHECKPOINTS_DIR=./models/checkpoints

# ---------------------------------------------------------------------------
# Disk Image Data Directory (HOST path)
# Folder containing your raw disk/storage images (.img, .dd, .raw, etc.)
# These are mounted READ-ONLY inside the container at /data/
#
# In the Streamlit UI (Configuration page), enter the CONTAINER path:
#   /data/your_disk_image.img
#
# Windows examples:
#   DATA_DIR=C:/forensics/disk_images
#   DATA_DIR=D:/evidence
#
# macOS/Linux examples:
#   DATA_DIR=/mnt/forensics
#   DATA_DIR=~/disk_images
# ---------------------------------------------------------------------------
DATA_DIR=./dataset

# ---------------------------------------------------------------------------
# Recovered Data Output Directory (HOST path)
# Folder where exported/recovered files will be written.
# This folder is writable inside the container at /app/recovered_data/
#
# Windows examples:
#   RECOVERED_DIR=C:/recovered_output
#   RECOVERED_DIR=./recovered_data
#
# macOS/Linux examples:
#   RECOVERED_DIR=~/recovered_data
#   RECOVERED_DIR=./recovered_data
# ---------------------------------------------------------------------------
RECOVERED_DIR=./recovered_data
