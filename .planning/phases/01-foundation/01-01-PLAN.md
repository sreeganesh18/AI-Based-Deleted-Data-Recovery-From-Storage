---
phase: 01-foundation
plan: 01-01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, utils/metadata.py]
autonomous: true
requirements: [ENV-01, DATA-01]
must_haves:
  truths:
    - "Project dependencies are managed by uv"
    - "Fragments can be tracked with unique IDs and source offsets"
  artifacts:
    - "uv.lock"
    - "utils/metadata.py"
  key_links:
    - "utils/metadata.py -> dataset/metadata.json"
---
<objective>Setup environment and data tracking.</objective>
<tasks>
  <task type="auto">
    <name>Task 1: uv Sync</name>
    <action>Run uv sync and verify python version.</action>
    <files>[pyproject.toml]</files>
    <verify>uv run python --version</verify>
    <done>[uv.lock]</done>
  </task>
  <task type="auto">
    <name>Task 2: Metadata Manager</name>
    <action>Implement utils/metadata.py to handle fragment tracking in SQLite/JSON.</action>
    <files>[utils/metadata.py]</files>
    <verify>python -m pytest tests/test_metadata.py</verify>
    <done>[utils/metadata.py]</done>
  </task>
</tasks>
